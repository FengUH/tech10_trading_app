# Local Streamlit secrets (may contain passwords or API keys)
.streamlit/

# Internal scripts that should not be exposed publicly
# These folders may contain credentials, tokens, or private logic.
alerts/
data_ingest/
strategy/

# Local data, images, and documents that are not intended for version control
*.csv
*.png
*.docx
~$*

# Standard Python ignores
__pycache__/
*.pyc
.env
.DS_Store
